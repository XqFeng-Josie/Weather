# Latent Codec 重建测试 (Reconstruction Testing)

本目录包含用于测试和评估不同 **Latent Codec（潜在空间编码器-解码器）** 对天气数据重建能力的完整工具集。

## 📖 概述

在 WeatherDiff 项目中，我们使用 Latent U-Net 架构进行天气预测，该架构需要先将天气图像编码到潜在空间，然后在潜在空间进行预测，最后解码回像素空间。因此，**编码器-解码器的重建质量直接影响预测性能**。

本模块提供了完整的测试流程，用于评估以下 Latent Codec 的重建能力：

### 支持的 Latent Codec

1. **VAE (Stable Diffusion VAE)** ⭐
   - 预训练的 Stable Diffusion VAE
   - 无需训练，开箱即用
   - 压缩比：512×512 → 64×64 (8×)

2. **RAE (Representation Autoencoder)** ⭐
   - 支持多种预训练编码器：
     - **DINOv2-B**: Vision Transformer，自监督学习
     - **DINOv2-B_decXL**: DINOv2-B + Finetune的解码器（更大的ViT解码器）
     - **DINOv2-B_decSmall**: DINOv2-B + Finetune的解码器（更小的ViT解码器）
     - **MAE**: Masked Autoencoder
     - **MAE_decXL**: MAE + Finetune的解码器（更大的ViT解码器）
     - **MAE_decSmall**: MAE + Finetune的解码器（更小的ViT解码器）
     - **SigLIP2**: Sigmoid Loss for Language-Image Pre-training
   - 需要从 RAE 项目下载预训练模型

3. **S-VAE (Hyperspherical Variational Autoencoder)**
   - 超球面变分自编码器
   - 需要从头训练，可针对天气数据优化
   - 使用 von Mises-Fisher 分布作为潜在空间分布



## 🚀 快速开始

### 1. 准备天气图像数据

所有 codec 测试都使用相同的数据准备流程：

```bash
cd reconstruction
python prepare_weather_images.py \
    --data-path gs://weatherbench2/datasets/era5/1959-2022-6h-64x32_equiangular_conservative.zarr \
    --variable 2m_temperature \
    --time-slice 2020-01-01:2020-12-31 \
    --target-size 256 256 \
    --output-dir weather_images \
    --n-workers 8 \
    --concurrent-interpolation \
    --norm-stats-path /path/to/existing/normalization_stats.json  # 可选：使用已有归一化参数
```

**参数说明：**
- `--data-path`: ERA5 zarr 数据路径
- `--variable`: 气象变量名（如 `2m_temperature`）
- `--time-slice`: 时间范围
- `--target-size`: 目标图像尺寸（H W）
- `--output-dir`: 输出目录
- `--n-workers`: 并发工作进程数
- `--concurrent-interpolation`: 启用并发插值
- `--norm-stats-path`: 使用已有的归一化参数（可选）

**输出：**
- `weather_images/`: 图像文件目录（PNG格式，0-255）
- `weather_images/normalization_stats.json`: 归一化参数（用于后续反归一化到物理单位）

### 2. 测试 VAE 重建

```bash
# 从 Weather 项目根目录运行
cd /path/to/Weather
python reconstruction/test_vae_reconstruction.py \
    --data-path reconstruction/weather_images \
    --n-test-samples 100 \
    --save-separate
```

**参数说明：**
- `--data-path`: 图像目录路径（必需）
- `--n-test-samples`: 测试样本数量（-1 表示使用所有数据）
- `--save-separate`: 分别保存原图和重建图到子文件夹

**输出：**
- `outputs/vae_reconstruction/vae_reconstruction_results.json`: 评估指标
- `outputs/vae_reconstruction/original/`: 原图目录
- `outputs/vae_reconstruction/reconstructed/`: 重建图目录
- `outputs/vae_reconstruction/world_maps/`: 世界地图可视化

### 3. 测试 RAE 重建

RAE 支持批量测试多个 encoder：

```bash
cd reconstruction
bash test_rae_reconstruction.sh
```

**前置要求：**
1. 确保 RAE 项目已安装并配置好环境
2. 下载 RAE 预训练模型：

```bash
cd RAE
pip install huggingface_hub
hf download nyu-visionx/RAE-collections --local-dir models
```

**测试的 encoder：**
- DINOv2-B
- DINOv2-B_decXL (更大的ViT解码器)
- DINOv2-B_decSmall(更小的ViT解码器)
- MAE
- MAE_decXL (更大的ViT解码器)
- MAE_decSmall(更小的ViT解码器)
- SigLIP2

**输出：**
- `outputs/rae_reconstruction/recon_samples_*/`: 各encoder的重建结果
- `outputs/rae_reconstruction/recon_merged/comparison_4samples_*.png`: 对比可视化

### 4. 统一对比分析 ⭐

使用 `compare_reconstructions.py` 可以对比多个 codec 的重建效果：

```bash
python compare_reconstructions.py \
    --original-dir weather_images \
    --reconstructed-dirs \
        outputs/vae_reconstruction/reconstructed \
        outputs/rae_reconstruction/recon_samples_DINOv2-B/RAE-pretrained-bs4-fp32 \
        outputs/rae_reconstruction/recon_samples_MAE/RAE-pretrained-bs4-fp32 \
    --labels VAE RAE-DINOv2-B RAE-MAE \
    --output comparison_all.png \
    --metrics-output metrics_all.json \
    --denormalize
```

**参数说明：**
- `--original-dir`: 原始图像目录
- `--reconstructed-dirs`: 多个重建结果目录（可指定多个）
- `--labels`: 对应的标签（用于图例）
- `--output`: 输出对比图路径
- `--metrics-output`: 输出指标JSON文件
- `--denormalize`: 自动使用归一化参数反归一化到物理单位

**输出：**
- 对比可视化图像（包含原图、各codec重建图、误差图）
- 评估指标表格（RMSE, MAE, PSNR, SSIM, 相关系数等）
- 指标JSON文件

## 📊 评估指标

重建测试会计算以下指标：

### 图像空间指标（归一化后，0-255）
- **RMSE**: 均方根误差
- **MAE**: 平均绝对误差
- **PSNR**: 峰值信噪比（dB，越高越好）
- **SSIM**: 结构相似性指数（0-1，越高越好）

### 物理空间指标（反归一化后，物理单位）
- **RMSE**: 物理单位的均方根误差（如温度：K）
- **MAE**: 物理单位的平均绝对误差
- **相关系数**: 空间模式相似度（0-1，越高越好）

### 验收标准

- **RMSE < 10K 且 相关系数 > 0.9**: 重建质量良好 ✅
- **RMSE < 15K**: 重建质量一般，建议微调
- **RMSE > 15K**: 重建质量差，建议训练自定义编码器或使用其他codec

## 📈 实际测试结果

Train time_slice 2015-01-01:2019-12-31
Test time_slice 2020-01-01:2020-12-31 （1460 个）
基于 **1460 个测试样本**（2m_temperature 变量，256×256 分辨率）的重建测试结果：

### 物理空间指标对比（温度单位：K）

| Codec | RMSE (K) | MAE (K) | SSIM | 相关系数 | 状态 |
|-------|----------|---------|------|----------|------|
| **RAE-MAE_decSmall** ⭐ | **0.17** | **0.12** | - | - | ✅ 最佳 |
| **RAE-MAE** | **0.96** | **0.73** | **0.999** | **0.999** | ✅ 优秀 |
| **RAE-DINOv2-B_decSmall** | 1.20 | 0.83 | - | - | ✅ 良好 |
| **SD-VAE** | 2.46 | 2.13 | 0.998 | 0.998 | ✅ 良好 |
| **RAE-MAE_decXL** | 4.47 | 3.92 | 0.992 | 0.994 | ✅ 良好 |
| **RAE-DINOv2-B** | 5.03 | 3.99 | 0.971 | 0.974 | ✅ 良好 |
| **RAE-SigLIP2** | 6.86 | 5.65 | 0.977 | 0.977 | ⚠️ 一般 |
| **RAE-DINOv2-B_decXL** | 7.01 | 5.42 | 0.962 | 0.986 | ⚠️ 一般 |

### 归一化空间指标对比

| Codec | RMSE | MAE | PSNR (dB) | SSIM | 相关系数 |
|-------|------|-----|-----------|------|----------|
| **RAE-MAE** ⭐ | **0.007** | **0.006** | **42.81** | **0.999** | **0.999** |
| **RAE-MAE_decSmall** | **0.011** | **0.008** | - | - | - |
| **SD-VAE** | 0.016 | 0.015 | 36.03 | 0.999 | 0.999 |
| **RAE-MAE_decXL** | 0.036 | 0.031 | 28.90 | 0.991 | 0.995 |
| **RAE-DINOv2-B** | 0.039 | 0.031 | 28.14 | 0.973 | 0.973 |
| **RAE-SigLIP2** | 0.048 | 0.041 | 26.37 | 0.983 | 0.984 |
| **RAE-DINOv2-B_decSmall** | 0.057 | 0.040 | - | - | - |
| **RAE-DINOv2-B_decXL** | 0.058 | 0.044 | 24.73 | 0.961 | 0.988 |

### 关键发现

1. **RAE-MAE_decSmall 表现最佳** ⭐
   - 物理空间 RMSE 仅 **0.17K**，远超其他 codec
   - 推荐用于高精度预测任务

2. **RAE-MAE 表现优秀** ⭐
   - 物理空间 RMSE **0.96K**，远低于验收标准（< 10K）
   - 相关系数达到 **0.999**，几乎完美重建
   - 归一化空间 RMSE **0.007**，PSNR **42.81 dB**
   - 推荐用于 Latent U-Net 训练

3. **RAE-DINOv2-B_decSmall 表现良好**
   - 物理空间 RMSE **1.20K**，优于原版 RAE-DINOv2-B（5.03K）
   - 归一化空间指标略低于原版，但物理空间表现更优

4. **SD-VAE 表现良好**
   - 物理空间 RMSE **2.46K**，满足验收标准
   - 归一化空间 RMSE **0.016**，PSNR **36.03 dB**
   - 开箱即用，无需额外训练
   - 适合快速原型开发

5. **RAE-MAE_decXL 表现良好**
   - 物理空间 RMSE **4.47K**，满足验收标准
   - Finetune 解码器后性能下降（相比 RAE-MAE）
   - 归一化空间 RMSE **0.036**，PSNR **28.90 dB**

6. **RAE-DINOv2-B 表现一般**
   - 物理空间 RMSE **5.03K**，满足验收标准但未达到最佳性能
   - 归一化空间 RMSE **0.039**，PSNR **28.14 dB**
   - 建议优先考虑 RAE-MAE 系列

7. **RAE-SigLIP2 和 RAE-DINOv2-B_decXL 表现较差**
   - 物理空间 RMSE 分别为 **6.86K** 和 **7.01K**，接近验收标准上限
   - 不建议用于高精度预测任务

### 推荐使用顺序

1. **RAE-MAE_decSmall** - 最佳重建质量（RMSE 0.17K），推荐用于生产环境
2. **RAE-MAE** - 优秀重建质量（RMSE 0.96K），推荐用于生产环境
3. **RAE-DINOv2-B_decSmall** - 良好性能（RMSE 1.20K），备选方案
4. **SD-VAE** - 开箱即用，适合快速实验（RMSE 2.46K）
5. **RAE-MAE_decXL** - 备选方案，性能良好（RMSE 4.47K）


## 🔧 环境要求

### Python 依赖

```bash
# Weather 项目依赖
pip install -r requirements.txt
# S-VAE 训练依赖
# pip install hyperspherical-vae

# 可视化（可选）
pip install cartopy

# 数据访问
pip install gcsfs zarr xarray
```

### RAE 项目

确保 RAE 项目已安装并配置好环境。RAE 测试需要在 RAE 项目目录下运行，或确保 RAE 相关代码在路径中。

## 📈 使用场景

### 1. 选择最佳 Latent Codec

在开始训练 Latent U-Net 之前，先测试不同 codec 的重建能力，选择重建质量最好的 codec。

### 2. 评估 Codec 适配性

不同 codec 对不同类型的气象数据（温度、位势高度、比湿等）可能有不同的表现，通过重建测试可以评估适配性。

### 3. 调试和优化

如果 Latent U-Net 预测效果不佳，可能是 codec 重建质量的问题。通过重建测试可以定位问题。

### 4. 对比研究

对比不同 codec 的优缺点，为后续研究提供参考。

## 🎯 最佳实践

1. **数据准备**：
   - 使用足够的时间范围（至少1年）以确保数据多样性
   - 保存归一化参数，确保后续测试使用相同的归一化

2. **测试样本**：
   - 使用足够的测试样本（建议 ≥ 100）以获得可靠的统计结果
   - 可以随机采样或使用特定时间段的样本

3. **对比分析**：
   - 使用 `compare_reconstructions.py` 进行统一对比，确保公平比较
   - 同时查看图像空间和物理空间的指标

4. **可视化检查**：
   - 除了数值指标，也要查看可视化结果，检查空间模式是否保持

## 📚 相关文档

- [主项目 README](../README.md) - Weather 项目总体说明
- [模型架构文档](../MODEL.md) - 详细模型架构说明

## 🔗 参考资源

- [RAE 项目](https://github.com/nyu-visionx/RAE) - Representation Autoencoder
- [Stable Diffusion](https://github.com/Stability-AI/stablediffusion) - Stable Diffusion VAE
- [S-VAE 论文](https://arxiv.org/abs/1804.00891) - Hyperspherical Variational Auto-Encoders

---

**提示**：建议先运行 VAE 和 RAE 的重建测试，对比结果后再决定使用哪个 codec 进行 Latent U-Net 训练。
