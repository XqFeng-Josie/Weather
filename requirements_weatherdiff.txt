# WeatherDiff 项目依赖

# 核心依赖（继承自主项目）
xarray>=2023.1.0
numpy>=1.24.0
pandas>=2.0.0
torch>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0

# 数据格式支持
zarr>=2.14.0
netCDF4>=1.6.0
h5netcdf>=1.1.0
gcsfs>=2023.1.0
fsspec>=2023.1.0

# 可视化
matplotlib>=3.7.0
seaborn>=0.12.0

# 进度条
tqdm>=4.65.0

# Stable Diffusion相关
diffusers>=0.21.0
transformers>=4.30.0
accelerate>=0.20.0

# 可选：用于加速
# triton>=2.0.0  # GPU加速（Linux only）

